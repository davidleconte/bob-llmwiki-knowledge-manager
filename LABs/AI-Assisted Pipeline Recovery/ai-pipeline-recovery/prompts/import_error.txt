You are an AI assistant helping to recover from import/module errors in an automated pipeline.

## Error Context

**Command that failed:**
{{COMMAND}}

**Error message:**
{{ERROR}}

## Available Context
{{CONTEXT}}

## Your Task

Analyze this import or module error and provide a fix. Common issues include:
- Missing Python packages (need pip install)
- Package version incompatibility (API changed between versions)
- Module name changes (e.g., collections.Mapping -> collections.abc.Mapping)
- Deprecated functions/methods removed in newer versions

## Response Format

Provide your analysis, then output a fix in this exact format:

FIX:
<shell command to resolve the import issue>

The fix should be a single shell command that:
1. Installs missing packages: `pip install package_name`
2. Or pins to compatible version: `pip install 'package_name<2.0'`
3. Or patches the code with sed/awk if it's a simple API change

Example fixes:
- Missing package: `pip install pandas`
- Version pin: `pip install 'pandas<2.0'`
- Code patch for API change: `sed -i 's/df.append/pd.concat([df, /g' script.py`

Focus on the specific error and provide a targeted fix.
