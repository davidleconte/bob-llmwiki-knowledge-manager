# AI-Assisted Pipeline Recovery

A short lab on building self-healing data pipelines, where an AI agent automatically diagnoses failures, suggests contextual fixes, and applies them — turning 3 AM incident response into automated remediation. You run a pipeline that recovers from schema mismatches with the IBM Bob CLI and learn the safety mechanisms that prevent dangerous auto-fixes. (~15–20 min)
