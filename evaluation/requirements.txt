# NOTE (single-home discipline): this is NOT the project's dependency source.
# The package's runtime/dev dependencies live once in pyproject.toml and are
# pinned in uv.lock (the reproducible closure CI audits). This file lists only
# the OPTIONAL, analysis-only tooling for the ad-hoc adversarial-review scripts
# under evaluation/; it is intentionally separate, and is not installed by CI,
# by the test suite, or by the package.
#
# A pinned lockfile (evaluation/requirements.lock.txt) is generated from this
# file via: uv pip compile evaluation/requirements.txt --universal -o evaluation/requirements.lock.txt
# The lockfile is audited by the sbom CI job (pip-audit, continue-on-error).
# Regenerate after updating version floors here.
#
# Adversarial Review Framework Dependencies
# Install with: pip install -r evaluation/requirements.lock.txt  (pinned, preferred)
# Or floating:  pip install -r evaluation/requirements.txt
# Or with uv:   uv pip install -r evaluation/requirements.lock.txt

# Core data analysis
pandas>=3.0.0
numpy>=1.24.0

# Statistical analysis
scipy>=1.10.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.2

# Optional: Enhanced reporting
# tabulate>=0.9.0  # For prettier markdown tables (optional)

# Python version requirement
# python_requires>=3.11
