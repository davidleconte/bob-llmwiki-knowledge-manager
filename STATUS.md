# Project Status — Single Source of Truth

> **This file is the canonical maturity status for the repository.** Any other document that states a maturity level must defer to this file. Divergence is a bug (a "one home per value" CI validator, `scripts/check_status_consistency.py`, enforces this automatically). Historical/dated snapshots under `docs/**` and `evaluation/**` reflect what was believed at their date and are **not** authoritative.

| Field | Value |
|---|---|
| **Overall status** | **Beta — Not Production Ready** |
| **Maturity** | Adversarial remediation in progress (2026-07-19 audits) — see `adversarial-remediation-plan.md` |
| **As of** | 2026-07-19 |
| **Basis** | [Independent Counter-Audit 2026-07-19](docs/knowledge-base/research/counter-audit-2026-07-19-independent.md) · [Adversarial Audit 2026-07-19](docs/knowledge-base/research/adversarial-audit-2026-07-19.md) · [Phase-8 Adversarial Re-Audit 2026-07-14](docs/knowledge-base/research/audit-2026-07-14-signoff.md) · [Institutional Audit 2026-07-13](docs/knowledge-base/research/audit-2026-07-13-institutional.md) |
| **Weighted grade vs institutional bar** | **No current independent grade.** The prior self-assessed A+ has been **withdrawn** — self-grading is not a substitute for independent verification. On-file independent verdicts: counter-audit (2026-07-19) **2.9/5**; last graded verdict **NO-GO 3.46/4.3** (2026-07-14). An independent re-grade is pending closure of the remaining Critical/High findings. |
| **Roadmap** | Phases 0–8 complete + gap closure (2026-07-18). **Wave 0/R/1 remediation done (2026-07-19, branch `remediation/engineering-soundness-2026-07-19`):** tree is gate-green; retrieval-wiring / never-empty-optimiser / validate-kb bugs fixed (incl. real latent bugs mypy caught — the index was constructed without its embedder, silently masking a keyword-only fallback); 15 broken KB refs repaired. **2026-07-19 adversarial audits** (46 findings, 19 CONFIRMED exploits): adversarial remediation plan at `adversarial-remediation-plan.md` — 14 sub-tasks, H1 (Reconnect & Repair, days), H2 (Lifecycle & Trust, weeks). New test packages (committed 2026-07-19 on the remediation branch): `tests/security/` (path containment, cache integrity, prompt-injection boundary, trust-tier), `tests/gates/` (CI gate baseline, savings gate, status gate, validate-kb gate), `tests/retrieval/` (ranking normalization, wiring), `tests/performance/test_dos_hardening.py`, `tests/optimizer/test_optimizer_integrity.py`. **Prior milestones (2026-07-18):** G-1–G-4 closed; `docs/sla.md` v1.0; `tests/load/` 8 load+soak tests; `sentence-transformers>=3.0` in dev extras; `src/embeddings/`, `src/graph/`, P3/P4 query quality shipped and validated. |

## What "Beta — Not Production Ready" means here

- **Headline savings are now measured, manifest-backed (Phase 5).** The earlier "68.96% / 95% CI / VALIDATED" figures were fabricated by a simulation that never invoked the optimizer (see [VALIDATION_DISCLAIMER.md](evaluation/validation-disclaimer.md)) and remain withdrawn. Phase 5 replaced that with a real harness (`python -m src.validation`): optimizer compression measures **~20% mean savings** (95% CI ≈ [19%, 21%]) over N=183 real in-repo documents with a passing null test; cache recompute-avoidance and lossy truncation are reported **separately**. Provenance: `evaluation/results/validation-2026-07-14/` (`report.json` + `manifest.json`).
- **The Python token optimizer now has a unified facade/CLI (Phase 4, done).** Its cache/optimizer/truncation/monitoring are composed behind a single `TokenOptimizer` facade and a `bob-optimize` CLI (`python -m src`), with configuration wired to the runtime. The Bash "Bob Shell KB manager" remains a separate product; merging the two is not in scope.
- **The Critical/High correctness bugs the institutional audit found (Dimension C: C1–C7 + the RLock deadlock) are fixed**, each guarded by a behavioral regression test that fails if the fix is reverted (see `CHANGELOG.md` → Fixed). Lower-severity residuals are tracked in the Phase-8 sign-off audit. **2026-07-19 audits added new Critical/High findings** (CODE-01–CODE-08, MEM-02–MEM-04, ATK-FS-01/02, ATK-MEM-01, ATK-GATE-01–03, CLM-01/02) — tracked in `adversarial-remediation-plan.md`; remediation in progress.
- **CI enforces the quality gates.** Coverage gate + per-package floors, ruff lint/format, mypy, a 3.11/3.12 matrix, the flag-gated e2e suite, the manifest-backed validation harness (null + manifest + tiktoken gates) and its savings-claim guard, benchmark-regression trending, an SBOM, and a `src→scripts` layering gate all run in CI. Dependency locking, the lint/type gates, and the reconciled Python matrix landed in Phase 3; the facade/CLI + config→runtime wiring in Phase 4; the real, manifest-backed validation in Phase 5. **Update (2026-07-19, Wave 0):** the tree now passes all its own gates — `check_savings_claims`, `check_status_consistency`, `check_value_homes`, `validate-kb`, `ruff` and `mypy` are green, and the two prior test failures are resolved.

## Terminology guardrail

- **"Coverage"** = code coverage measured by `pytest --cov`, enforced by the `fail_under` gate in `pyproject.toml` (the single home for the number: **≥80%**). It is **not** the same as **test pass rate**. Do not conflate them. Point-in-time snapshot (2026-07-19, post-Wave-1): **89.12% global** (ex load/perf) — `src/tools/` and `src/delegation/` both fold into the gated denominator with per-package floors (`scripts/check_coverage_by_package.py`); **1189 passed / 23 skipped** (performance/load tests excluded from the coverage run due to benchmark harness contention; they pass when run in isolation). The 2026-07-19 test packages (`tests/security/`, `tests/gates/`, `tests/retrieval/`, `tests/performance/test_dos_hardening.py`, `tests/optimizer/test_optimizer_integrity.py`) are now committed and counted. Undated maturity claims elsewhere must defer to this file; a CI validator (`scripts/check_status_consistency.py`) enforces that the cited gate matches `pyproject.toml`.
- **Every published savings/cost number must cite a reproducible run with a manifest** (data hash, code SHA, config, seed, library versions, `git_dirty`). Numbers without provenance are not to be published.

## Not claimed

Enterprise SLAs, production support, guaranteed savings percentages, automated multi-agent research, or Windows compatibility.
