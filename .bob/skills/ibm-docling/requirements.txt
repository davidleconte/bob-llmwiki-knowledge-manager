# ibm-docling — tiered dependencies. Install only the tier your task needs.
# ---------------------------------------------------------------------------
# Tier 1 — core: convert / OCR / tables / structure / chunking (CPU, no GPU).
#   pip install -r requirements.txt
docling>=2.55.0
docling-core>=2.60.0

# Tier 2 — VLM pipeline (local vision-language models: GraniteDocling/SmolDocling).
#   pip install "docling[vlm]"        # pulls torch + transformers + accelerate
# transformers>=4.44
# accelerate>=0.34
# torch                                 # match your CUDA/CPU/MPS build

# Tier 3 — Apple Silicon MLX backend for the VLM pipeline.
#   pip install mlx mlx-lm

# Tier 4 — ASR pipeline (audio/video transcription via Whisper).
#   pip install "docling[asr]"        # or: pip install openai-whisper

# Tier 5 — RAG tokenizers / integrations.
#   pip install "docling-core[chunking-openai]"   # OpenAI tiktoken tokenizer
#   pip install langchain-docling                  # LangChain loader
#   pip install llama-index-readers-docling llama-index-node-parser-docling

# Tier 6 — alternate OCR engines (default EasyOCR needs nothing extra).
#   pip install rapidocr onnxruntime      # RapidOCR
#   pip install tesserocr                 # + system tesseract
#   pip install ocrmac                    # macOS Vision (macOS only)

# Used only by scripts/selftest.sh to synthesize a sample PDF:
#   pip install reportlab
