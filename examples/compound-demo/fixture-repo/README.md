# Compound-demo fixture repo (pinned)

A tiny, **frozen** sample project analyzed by the two-session compound demo. It is
vendored here (inside the repo, so containment holds without `--allow-external`) at a
fixed content so the demo is reproducible. Do not edit it to change a demo result.
